# Base URL for the Flask backend (restaurant-web-app). Leave as /api if you
# add a reverse proxy in front of both apps, or point it directly at Flask, e.g:
# VITE_API_BASE_URL=http://localhost:5000/api
VITE_API_BASE_URL=/api
